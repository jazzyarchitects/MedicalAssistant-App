<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <Preference android:title="Clear Data"
        android:summary="Clear all saved data"
        android:key="delete_database" >

        </Preference>

    <Preference
        android:title="Export"
        android:summary="Export data to external memory"
        android:key="exportPref">
        </Preference>

    <Preference android:title="Import"
        android:key="importPref"
        android:summary="Import data from external memory">
        </Preference>

</PreferenceScreen>
